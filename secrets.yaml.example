# Copy to secrets.yaml and fill in. secrets.yaml is gitignored.
#
# Govee OpenAPI key. Get one from the Govee Home app:
#   User -> About Us -> Request API Key   (arrives by email in seconds)
# The same key is used for the REST API and as BOTH username and password
# for the MQTT event stream.
api_govee_key: "REPLACE_ME_GUID"
